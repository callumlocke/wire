# wire

Experimental library for composing functional build pipelines with caching to make incremental rebuilds fast.

The goal is to get the fastest possible feedback loop when developing complex front-end applications with niche build requirements.
